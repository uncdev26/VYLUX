{
  "name": "@newlight/database",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "generate": "supabase gen types typescript --local > src/types.ts",
    "migrate": "supabase db push"
  }
}
