{
  "name": "newlight",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "svelte": "^5.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
